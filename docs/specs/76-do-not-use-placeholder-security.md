# DO NOT USE PLACEHOLDER SECURITY

Never write:

```text
password === "admin123"
```

Never write:

```text
isAdmin = true
```

Never write:

```text
if (user.role === "admin") {
   // trust frontend
}
```

Security must be real.

---
